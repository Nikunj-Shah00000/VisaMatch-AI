import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { api } from '../lib/api';
import { setSession } from '../lib/auth';
export default function Signup(){const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [visaStatus,setVisaStatus]=useState('OPT');const [skills,setSkills]=useState('React, TypeScript, Node.js, PostgreSQL');const [deadline,setDeadline]=useState('');const [resumeText,setResumeText]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const iso=deadline?new Date(deadline).toISOString():undefined;const {data}=await api.post('/auth/signup',{email,password,role:'CANDIDATE',visaStatus,skills:skills.split(',').map(s=>s.trim()).filter(Boolean),visaClockDeadline:iso,resumeText});setSession(data.token,data.user);nav('/dashboard')}catch(err:any){setError(err.response?.data?.message||'Could not create account.')}finally{setLoading(false)}}return <main className="grid min-h-screen place-items-center px-6"><form onSubmit={submit} className="panel w-full max-w-lg p-8"><h1 className="text-3xl font-bold">Create your VisaMatch profile</h1><p className="mt-2 text-slate-400">Make sponsorship signals and your visa timeline visible in one place.</p>{error&&<div className="mt-5 rounded-xl border border-rose-900 bg-rose-950/30 p-3 text-sm text-rose-300">{error}</div>}<label className="mt-6 block text-sm">Email<input type="email" required className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block text-sm">Password<input type="password" required minLength={8} className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></label><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm">Visa status<select className="input mt-2" value={visaStatus} onChange={e=>setVisaStatus(e.target.value)}><option>OPT</option><option>CPT</option><option>H1B</option></select></label><label className="text-sm">Clock deadline<input type="datetime-local" className="input mt-2" value={deadline} onChange={e=>setDeadline(e.target.value)}/></label></div><label className="mt-4 block text-sm">Resume text<textarea className="input mt-2 min-h-32" value={resumeText} onChange={e=>setResumeText(e.target.value)} placeholder="Paste your resume text here for contextual matching."/></label><label className="mt-4 block text-sm">Skills<input className="input mt-2" value={skills} onChange={e=>setSkills(e.target.value)}/><span className="mt-1 block text-xs text-slate-500">Comma-separated.</span></label><button className="btn-primary mt-6 w-full" disabled={loading}>{loading?'Creating…':'Create account'}</button><p className="mt-6 text-center text-sm text-slate-400">Already have an account? <Link className="text-cyan-400" to="/login">Sign in</Link></p></form></main>}
