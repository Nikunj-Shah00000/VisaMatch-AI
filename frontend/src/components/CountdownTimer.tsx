import { useEffect, useState } from 'react';
import { Clock3 } from 'lucide-react';
export default function CountdownTimer({ deadline }: { deadline?: string | null }) { const [now, setNow] = useState(Date.now()); useEffect(() => { const id = setInterval(() => setNow(Date.now()), 1000); return () => clearInterval(id); }, []); if (!deadline) return <div className="panel p-6"><div className="flex items-center gap-3"><Clock3 className="text-cyan-400"/><div><p className="font-semibold">Visa Clock</p><p className="text-sm text-slate-400">Set your OPT/CPT deadline to activate the countdown.</p></div></div></div>; const diff = new Date(deadline).getTime() - now; const positive = Math.max(0, diff); const days = Math.floor(positive / 86400000); const hours = Math.floor(positive / 3600000) % 24; const minutes = Math.floor(positive / 60000) % 60; const seconds = Math.floor(positive / 1000) % 60; return <div className="panel overflow-hidden p-6"><div className="flex items-center justify-between"><div><p className="text-sm text-slate-400">Visa Clock</p><p className="mt-1 text-2xl font-bold">{days}d {hours}h {minutes}m {seconds}s</p></div><Clock3 className={positive < 14 * 86400000 ? 'text-rose-400' : 'text-cyan-400'} size={32}/></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full bg-cyan-400" style={{ width: `${Math.max(3, Math.min(100, days / 180 * 100))}%` }}/></div><p className="mt-2 text-xs text-slate-500">Deadline: {new Date(deadline).toLocaleString()}</p></div> }
