import { Link, useNavigate } from 'react-router-dom';
import { BriefcaseBusiness, LayoutDashboard, LogOut, Sparkles } from 'lucide-react';
import { clearSession, getUser } from '../lib/auth';
export default function Navbar() { const nav = useNavigate(); const user = getUser(); return <header className="sticky top-0 z-20 border-b border-slate-800 bg-slate-950/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4"><Link to="/dashboard" className="flex items-center gap-2 font-bold"><Sparkles className="text-cyan-400" size={22}/>VisaMatch <span className="text-cyan-400">AI</span></Link><nav className="flex items-center gap-2 text-sm"><Link className="rounded-lg px-3 py-2 hover:bg-slate-900" to="/dashboard"><LayoutDashboard size={16} className="mr-1 inline"/>Dashboard</Link><Link className="rounded-lg px-3 py-2 hover:bg-slate-900" to="/jobs"><BriefcaseBusiness size={16} className="mr-1 inline"/>Job Board</Link><button className="rounded-lg px-3 py-2 text-slate-400 hover:bg-slate-900 hover:text-white" onClick={() => { clearSession(); nav('/login'); }}><LogOut size={16} className="mr-1 inline"/>Sign out</button></nav></div></header> }
