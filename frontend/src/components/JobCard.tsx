import { CheckCircle2, Send } from 'lucide-react';
import type { Job } from '../types';
export default function JobCard({ job, onApply, applying }: { job: Job; onApply: () => void; applying: boolean }) { return <article className="panel p-6"><div className="flex items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-xl font-semibold">{job.title}</h3>{job.company.verifiedSponsor && <span className="rounded-full bg-emerald-400/10 px-2.5 py-1 text-xs font-medium text-emerald-300"><CheckCircle2 size={13} className="mr-1 inline"/>Verified sponsor</span>}</div><p className="mt-1 text-sm text-slate-400">{job.company.name} · {job.company.industry}</p></div>{job.requiresSponsorship && <span className="rounded-full bg-cyan-400/10 px-3 py-1 text-xs text-cyan-300">{job.sponsorshipTrack || 'Sponsorship signal'}</span>}</div><p className="mt-5 line-clamp-3 text-sm leading-6 text-slate-300">{job.description}</p><div className="mt-5 flex flex-wrap gap-2">{job.skillsRequired.map(s => <span key={s} className="rounded-lg border border-slate-700 px-2.5 py-1 text-xs text-slate-300">{s}</span>)}</div><button onClick={onApply} disabled={applying} className="btn-primary mt-6"><Send size={16} className="mr-2 inline"/>{applying ? 'Applying…' : 'Apply & Match with AI'}</button></article> }
